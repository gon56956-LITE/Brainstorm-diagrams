# Testcases

Maintained regression cases are grouped by diagram type.

- `fishbone/`: fishbone renderer inputs and expected SVG outputs.
- `fault-tree/`: fault tree renderer inputs and expected SVG outputs.

Future diagram types should add their own subdirectory instead of mixing files at this level.
